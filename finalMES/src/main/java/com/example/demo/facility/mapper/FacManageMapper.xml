<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.facility.mapper.FacManageMapper">
	<select id="getfacList" resultType="FacManageVO">
	SELECT fm.*, p.*
	FROM fac_manage fm
	JOIN process p ON fm.proc_code = p.proc_code
	</select>
	
	<delete id="deleteFac" parameterType="String">
	DELETE FROM fac_manage
	WHERE fac_code IN (SELECT f.fac_code
	FROM fac_manage f
	JOIN process p ON f.proc_code = p.proc_code
	WHERE facCode = #{fac_code}
	</delete>
</mapper>
